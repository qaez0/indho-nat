import { Content } from '../../component';

const SetWithdrawPass = () => (
  <Content
    label={`Click the 'Set Withdraw Password' button below to create your withdrawal password. Once set, you'll be able to submit your withdrawal request.`}
    content={<></>}
    labelStyle={{
      fontFamily: 'Montserrat, sans-serif',
      fontSize: 12,
      fontWeight: 400,
    }}
  />
);

export default SetWithdrawPass;
